kind: Service
apiVersion: v1
metadata:
  name: cicd-artifactory-dev
spec:
  selector:
    app: artifactory
  ports:
  - protocol: TCP
    port: 80 # DEFAULT it is not necessary to add it
    targetPort: 8082